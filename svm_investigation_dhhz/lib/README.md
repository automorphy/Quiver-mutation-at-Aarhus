# Shared Libraries

*   **`svm_utils.py`**: Contains reusable functions for:
    *   Computing polynomial kernels with arbitrary precision integers.
    *   Cosine normalization (and option to disable it).
    *   Training custom SVM models and reporting metrics.
